export const environment = {
    production: false,
    logging: true,                      // Enable verbose logging in dev mode
    debugMode: true,                     // Additional debug settings, if needed
    featureFlags: {
        featureX: true,                    // Enable specific features for dev testing
        featureY: false
    }
};
